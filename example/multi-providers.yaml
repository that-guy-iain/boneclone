 providers:
   - provider: github
     username: that-guy-iain
     org: billabear
     token: fake-token
   - provider: gitlab
     username: bb-iain
     org: name
     token: gitlab-token
   - provider: azure
     username: azure-devops-username
     org: "https://dev.azure.com/example/"
     token: azure-token
files:
  include:
    - ci
  exclude:
    - ci/mocks.sh
identifier:
   filename: .boneclone
   content: golang

git:
  # When true, use PR flow (placeholder for now). When false, push directly.
  pullRequest: false
  # When set, commits will be pushed to this branch (created if missing)
  targetBranch: main